pipeline {
   agent any
   tools {nodejs "node"}
 

   stages {
      stage('Cloning Git') {
         steps {
            git 'https://github.com/CrisAlva25/ADYS2_ProyectoClase'
         }
      }
         
      stage('Install dependencies') {
         steps {
            ssh 'npm install'
         }
      }
      
      stage('Test') {
         steps {
            echo 'Testing... hola mundo'
         }
      }
      stage('Deploy') {
         steps {
            echo 'Deploying...'
            ssh 'ubuntu@ec2-18-222-17-215.us-east-2.compute.amazonaws.com'
            ssh 'cd ~/ADYS2_ProyectoClase'
            ssh 'git pull origin master'
         }
      }
   }
}
