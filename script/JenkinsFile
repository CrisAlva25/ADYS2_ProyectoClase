pipeline {
   agent any
   tools {nodejs "node"}
 

   stages {
      stage('Cloning Git') {
         steps {
            git 'https://github.com/CrisAlva25/ADYS2_ProyectoClase'
         }
      }
         
      stage('Install dependencies') {
         steps {
            sh 'npm install'
         }
      }
      
      stage('Build'){
         steps{
            echo 'Building...'
            sh 'ssh ubuntu@ec2-18-222-17-215.us-east-2.compute.amazonaws.com "sudo date && hostname"'
            sh 'ssh ubuntu@ec2-18-222-17-215.us-east-2.compute.amazonaws.com "sudo cd  ADYS2_ProyectoClase && git pull origin master"'
            sh 'ssh ubuntu@ec2-18-222-17-215.us-east-2.compute.amazonaws.com "sudo cd  ADYS2_ProyectoClase/ParqueoWeb && npm run ng build --prod && pm2 restart all"'
         }
      }
      
      stage('Test') {
         steps {
            echo 'Testing... hola mundo'
         }
      }
      
      stage('Deploy') {
         steps {
            echo 'Deploying...'
         }
      }
   }
}
